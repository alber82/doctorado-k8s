---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: metricschedulers.batch.uclm.es
spec:
  group: batch.uclm.es
  names:
    kind: MetricScheduler
    listKind: MetricSchedulerList
    plural: metricschedulers
    singular: metricscheduler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: MetricScheduler is the Schema for the metricschedulers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MetricSchedulerSpec defines the desired state of MetricScheduler
            properties:
              foo:
                description: Foo is an example field of MetricScheduler. Edit metricscheduler_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: MetricSchedulerStatus defines the observed state of MetricScheduler
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
